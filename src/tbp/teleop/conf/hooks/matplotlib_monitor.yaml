# @package experiment.config.step_hook
# Legacy renderer kept during web-UI rollout for side-by-side behavior validation.
_target_: tbp.teleop.default.Default
plotter:
  _target_: tbp.teleop.live_plotter.LivePlotter
  interactive: false
  max_delay: 2.0
